{% extends 'base.html.twig' %}

{% block body %}

{% for client in clients %}
    <div>
        <img src="/uploads/{{client.id}}.png" style="width:40px">
        {{ client.nom }} - {{ client.groupe?client.groupe.nom:'' }}
        </div>
{% endfor %}

{% endblock %}
